#include<stdio.h>
int main()
{
	int num,q,rnum=0;
	printf("enter the number");
	scanf("%d",&num);
	while(num>0)
	{	
	q=num%10;
	rnum=rnum*10+q;
	num=num/10;
	}
	printf("the reversed number is %d",rnum);
}
