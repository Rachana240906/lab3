#include<stdio.h>
int main()
{
	int n,i,q=1;
	printf("enter any number");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(i>q)
		q=i;
	}
	printf("the greatest of the numbers is %d",q);
 	printf("\nthe second greatest of the numbers is %d",q-1);
 }
